<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#135b5b"
                android:centerColor="#135b5b"
                android:centerY="1.0"
                android:endColor="#135b5b"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#3a96c9"
                    android:centerColor="#3a96c9"
                    android:centerY="1.0"
                    android:endColor="#3a96c9"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list> 